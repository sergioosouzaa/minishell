<p align="center">
  <img src="https://user-images.githubusercontent.com/80131514/229181398-49b6a3f6-9b4f-480d-a587-a9fd33a112fe.gif" />
</p>

<p align="center">
  <img src="https://user-images.githubusercontent.com/80131514/228539969-ceb8c6ef-1d93-4ebb-bfda-c3aac19e972a.png" />
</p>

This project was developed with <a href="https://github.com/thaisfsousa">@thaisfsousa</a>

## English

The Minishell project from 42 is a command-line interpreter that replicates the basic functionalities of a shell. This project is part of the curriculum for 42's software engineering program and aims to improve students' knowledge of system calls and processes.

## Project Description

The project is to create a shell that is able to execute simple commands, handle environment variables, and redirections. It must support the following built-in commands:

- echo with -n option
- cd with only a relative or absolute path
- pwd without any options
- export without any options
- unset without any options
- env without any options
- exit without any options

## Features

The Minishell project offers a variety of features, including:

- Prompt display with current working directory information
- Execution of basic commands such as `ls`, `echo`, `cd`, `env`, and `pwd`
- Error handling with appropriate error messages
- Inhibitors handling with `"` and `'`
- Redirections handling with `>`, `>>`, `<`, `<<`, and `|`
- Environment variables handling with `$` and `?`
- Signals handling with `ctrl-C`, `ctrl-D`, and `ctrl-\`

## How to Use

To use the Minishell project, you need to clone the project's repository from the 42 GitHub account. Once the repository is cloned, navigate to the project's root directory and run the `make` command to compile the program. After that, run the executable `./minishell` to start the shell.

## Conclusion

The Minishell project from 42 is an excellent way for software engineering students to deepen their knowledge of system calls and processes. It offers a wide range of features that replicate the basic functionalities of a shell, allowing users to perform various tasks from the command-line interface. It is a challenging but rewarding project that provides practical experience in Unix programming.

## Português

# Minishell

O projeto Minishell da 42 é um interpretador de linha de comando que replica as funcionalidades básicas de um shell. Este projeto faz parte do currículo do programa de engenharia de software da 42 e tem como objetivo aprimorar o conhecimento dos alunos em chamadas de sistema e processos.

## Descrição do Projeto

O projeto é criar um shell capaz de executar comandos simples, lidar com variáveis de ambiente e redirecionamentos. Ele deve suportar os seguintes comandos internos:

- echo com a opção -n
- cd com apenas um caminho relativo ou absoluto
- pwd sem nenhuma opção
- export sem nenhuma opção
- unset sem nenhuma opção
- env sem nenhuma opção
- exit sem nenhuma opção

## Funcionalidades

O projeto Minishell oferece uma variedade de recursos, incluindo:

- Exibição do prompt com informações do diretório de trabalho atual
- Execução de comandos básicos como `ls`, `echo`, `cd`, `env` e `pwd`
- Manipulação de erros com mensagens de erro apropriadas
- Manipulação de inibidores com `"` e `'`
- Manipulação de redirecionamentos com `>`, `>>`, `<`, `<<`, e `|`
- Manipulação de variáveis de ambiente com `$` e `?`
- Manipulação de sinais com `ctrl-C`, `ctrl-D` e `ctrl-\`

## Como Usar

Para usar o projeto Minishell, é necessário clonar o repositório do projeto da conta do GitHub da 42. Depois que o repositório for clonado, navegue até o diretório raiz do projeto e execute o comando `make` para compilar o programa. Depois disso, execute o executável `./minishell` para iniciar o shell.

## Conclusão

O projeto Minishell da 42 é uma excelente maneira para os alunos de engenharia de software aprofundarem seus conhecimentos em chamadas de sistema e processos. Ele oferece uma ampla gama de recursos que replicam as funcionalidades básicas de um shell, permitindo que os usuários realizem várias tarefas na interface de linha de comando. É um projeto desafiador, mas gratificante, que fornece experiência prática em programação Unix.
